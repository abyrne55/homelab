# Shared group for services that need cross-UID hardlinks under /var/mnt/data/content (NFS-backed)
# Still used for subgid/namespace mapping; filesystem ownership on NFS is not enforced via this group
g content-writers 2000 -
