# Age out stale container_images_storage dirs from failed Podman image pulls.
# These are left in /var/tmp when a pull is interrupted (e.g. by disk-full).
# The 'e' type cleans without modifying ownership or mode of /var/tmp itself.
e /var/tmp - - - - 1d
