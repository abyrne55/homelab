[Unit]
# Ensure the configarr user session starts after Sonarr and Radarr user
# sessions are active. After= in user-level units cannot reference other
# users' units, so this ordering is enforced at the system level instead.
After=user@1055.service user@1054.service
