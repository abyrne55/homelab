[Unit]
# Ensure the jellyfin user session starts after the media data disk is mounted.
# After= in user-level units cannot reference system-level units, so this
# ordering is enforced here at the system level instead.
After=var-mnt-data.mount
