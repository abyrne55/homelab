version: 1
base_url: "http://host.containers.internal:8096"
system: {}
startup:
  completeStartupWizard: true
library:
  virtualFolders:
    - name: "Movies"
      collectionType: "movies"
      libraryOptions:
        pathInfos:
          - path: "/media/Movies"
# users, encoding, plugins, branding — add as needed
