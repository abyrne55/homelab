[Unit]
# Don't start the service until credentials have been synced
After=homelab-secrets-sync.service

# The service already has ConditionDirectoryNotEmpty=/etc/age/credentials
# so it won't start until credentials actually exist
