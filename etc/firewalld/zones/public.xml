<?xml version="1.0" encoding="utf-8"?>
<zone target="DROP">
  <short>Public</short>
  <description>For use in public areas. Only selected incoming connections are accepted.</description>

  <!-- Allow SSH -->
  <service name="ssh"/>

  <!-- Allow Jellyfin -->
  <port port="20520" protocol="tcp"/>

  <!-- Allow Caddy -->
  <port port="20510" protocol="tcp"/>

  <!-- Allow qBittorrent WebUI (via Gluetun pod) -->
  <port port="20530" protocol="tcp"/>

  <!-- Port forwarding: external 80 → internal 20510 for rootless Caddy (handles both IPv4/IPv6) -->
  <forward-port port="80" protocol="tcp" to-port="20510"/>
</zone>
