<?xml version="1.0" encoding="utf-8"?>
<zone target="DROP">
  <short>Public</short>
  <description>For use in public areas. Only selected incoming connections are accepted.</description>

  <!-- Allow SSH -->
  <service name="ssh"/>

  <!-- Allow Jellyfin -->
  <port port="8096" protocol="tcp"/>

  <!-- Allow test HTTPd -->
  <port port="8080" protocol="tcp"/>

  <!-- Port forwarding: external 80 → internal 8080 for rootless Caddy (handles both IPv4/IPv6) -->
  <forward-port port="80" protocol="tcp" to-port="8080"/>
</zone>
