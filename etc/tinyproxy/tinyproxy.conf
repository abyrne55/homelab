Port 8888
Timeout 600
MaxClients 100

# Allow any client on jellynet (internal network — no external access to proxy)
Allow 0.0.0.0/0

# Allowlist mode: deny all except domains listed in filter file
Filter "/etc/tinyproxy/filter"
FilterDefaultDeny Yes

# Log to stdout (captured by journald via the container)
LogLevel Info
