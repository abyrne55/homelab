# Temporary Files

If you need to temporarily store files somewhere, **always use the `.tmp/` directory at the root of the repository**. Never write to `/tmp/`.

Create `.tmp/` if it doesn't exist. Feel free to delete/recreate it as needed.
